"use server"

export const createExpense = async () => {
    try {
        
    } catch (error) {
        console.log(error)
        return { success: false, message: "Failed to create expense" }
    }
}